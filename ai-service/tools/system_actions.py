def reassign_tasks(employee_id):
    print(f"[ACTION] Reassigning tasks from {employee_id}")
    return True

def reduce_load(employee_id):
    print(f"[ACTION] Reducing workload for {employee_id}")
    return True

def recommend_break(employee_id):
    print(f"[ACTION] Break recommended for {employee_id}")
    return True

def delay_deadlines(employee_id):
    print(f"[ACTION] Deadlines delayed for {employee_id}")
    return True
